(def! not (fn* (b)
  (if b false true)))

(def! load-file (fn* (f)
  (eval
    (read-string
      (str
        "(do " (slurp f) "\nnil)")))))

; <=, >= to be implemented

(def! swap! (fn* [a, f]
  (reset! a (f (deref a)))))
